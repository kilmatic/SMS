@model IEnumerable<SMSWebPortal.Models.Parent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.parent_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.parent_surname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_no)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.parent_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.parent_surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_no)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.parent_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.parent_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.parent_id })
        </td>
    </tr>
}

</table>
